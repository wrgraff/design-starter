# PWA Icons

Drop generated icons here. `vite.config.ts` references:

- `icon-192.png` — 192×192, `purpose: 'any'`
- `icon-512.png` — 512×512, `purpose: 'any'`
- `icon-maskable-512.png` — 512×512, `purpose: 'maskable'` (with ~10% safe-zone padding)

Generate from a single high-resolution master image. See `docs/PWA.md` → _Icons_ for recommended generators.

This README is a placeholder — replace the icons before deploying.
